package ${cfg.managerImplPackage};

import ${package.Mapper}.${originalMapperName};
import ${cfg.managerPackage}.${originalEntityName}Manager;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

/**
 * <p>
 * ${table.comment!} Manager实现类
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
@Slf4j
@Component
@RequiredArgsConstructor
public class ${originalEntityName}ManagerImpl implements ${originalEntityName}Manager {

    private final ${originalMapperName} ${lowMapperName};


}
